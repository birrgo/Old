name: Send Push Notification

on:
  workflow_dispatch:
    inputs:
      push_title:
        description: 'Notification Title'
        required: true
        default: 'Fira Tech Updated!'
        type: string
      push_message:
        description: 'Notification Message'
        required: true
        default: 'A new version or item has been posted. Open the app to check it out!'
        type: string
      image_url:
        description: 'Image URL for Big Picture Popup (Optional)'
        required: false
        type: string

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send OneSignal Overlay Push
        run: |
          TITLE="${{ github.event.inputs.push_title }}"
          MESSAGE="${{ github.event.inputs.push_message }}"
          IMAGE_URL="${{ github.event.inputs.image_url || '' }}"

          PAYLOAD=$(cat <<EOF
          {
            "app_id": "${{ secrets.ONESIGNAL_APP_ID }}",
            "included_segments": ["Subscribed Users"],
            "data": {
              "custom_title": "$TITLE",
              "custom_message": "$MESSAGE",
              "custom_image": "$IMAGE_URL",
              "action": "trigger_overlay_popup"
            },
            "content_available": true,
            "priority": 10,
            "android_channel_id": "2c87aaaf-d24f-4855-a992-3d749a5a016f"
          }
          EOF
          )

          curl --request POST \
            --url https://onesignal.com/api/v1/notifications \
            --header "Authorization: Basic ${{ secrets.ONESIGNAL_REST_API_KEY }}" \
            --header "Content-Type: application/json" \
            --data "$PAYLOAD"
